fragment TaggedFaceData on Identity {
    id
    first_name: name
    last_name
    relationship
  	gender
    account {
        id
    }

    selfie: current_photo {
        ...PhotoData
    }

    hierarchy_settings(hierarchy_item_id: $folderId) {
        skip_tags_onboarding
    }
}